# CoreCoder 单机部署、升级与回滚 Runbook

## 1. 适用范围

当前版本定位于单机或受控开发机：一个 SQLite 任务库、一个或多个本机 Worker、可选 MCP Server。本文不宣称支持跨节点高可用。

## 2. 部署前检查

1. 使用 Python 3.10～3.13 创建独立虚拟环境。
2. 安装锁定版本的项目依赖，生产环境不要直接跟随未审核的最新版本。
3. 将 API Key 放入进程环境或密钥管理系统，不写入仓库和命令参数。
4. 运行 `corecoder-doctor --workspace <工作区>`，确认 Python、依赖、Git、目录权限和配置。
5. 运行全量测试、离线故障演练和工业冒烟流程。
6. 确认 `.corecoder/` 所在磁盘容量、备份路径和访问权限。

## 3. 最小运行目录

```text
application/
├─ .venv/                 # 独立 Python 环境
├─ .corecoder/
│  ├─ tasks.db            # 任务台账
│  ├─ approvals.json      # 单机审批快照
│  └─ audit/              # 脱敏工具审计
├─ evals/reports/         # 验收和故障演练报告
└─ workspace/             # Agent 被授权操作的代码目录
```

应用进程账户只应拥有这些必要目录的权限。禁止使用管理员账户运行 Agent；工作区与凭据目录应隔离。

## 4. 启动顺序

1. 启动所需 MCP Server，并确认工具发现成功。
2. 启动任务恢复器，执行一次过期租约回收。
3. 启动 Worker；不同 Server 或业务队列建议使用可区分的 Worker ID。
4. 导出一次任务指标，确认没有异常过期租约和历史积压。
5. 再开放任务入口。

停止时顺序相反：先停止接收新任务，再等待运行任务完成或请求取消，最后关闭 Worker 与 MCP Runtime。

## 5. 升级流程

1. 停止新任务进入，记录升级开始时间和当前版本。
2. 等待 `RUNNING`/`CANCEL_REQUESTED` 任务收敛；超时后按故障流程回收，不直接删除记录。
3. 使用 SQLite 在线备份接口或数据库提供的 `.backup` 能力备份任务库；启用 WAL 时不要只复制单个 `tasks.db` 文件。
4. 备份审批快照和审计目录，并计算归档校验值。
5. 在新虚拟环境安装目标版本，不覆盖旧环境。
6. 运行 Doctor、数据库迁移、关键测试和离线故障演练。
7. 小流量启动一个 Worker，确认指标正常后再恢复全部入口。

## 6. 回滚条件

出现以下任一情况应停止放量并评估回滚：

- Doctor 出现必需项失败；
- 数据库迁移失败或任务记录无法读取；
- 工业冒烟场景失败；
- 过期租约、失败率或最老排队时间持续升高；
- 审计日志无法生成或出现未脱敏凭据。

## 7. 回滚步骤

1. 关闭任务入口并停止新版本 Worker。
2. 保留现场日志、指标和数据库副本，不覆盖故障证据。
3. 如果升级只包含向后兼容代码，切回旧虚拟环境并使用原任务库。
4. 如果数据库 Schema 不向后兼容，恢复升级前备份；升级期间创建的任务必须单独导出和人工核对，不能静默丢弃。
5. 启动旧版本后执行 Doctor、任务列表检查和一轮工业冒烟。
6. 记录故障时间线、影响任务和处置人。

当前数据库迁移主要是增加字段和索引，但项目尚未实现通用向下迁移。因此不能声称数据库可以无损一键回滚。

## 8. 日常巡检

- 查看任务各状态数量和最老待处理时间；
- 检查过期租约是否被恢复器及时清理；
- 抽查失败任务的最后错误、事故报告和审计记录；
- 定期验证备份能够恢复，而不只是确认“备份文件存在”；
- 定期轮换 API Key，并验证旧 Key 已失效；
- 检查故障演练、评测集和依赖安全更新。

## 9. 当前未覆盖能力

- 没有跨机房容灾、主从数据库或分布式选主；
- 没有内置身份认证和企业统一权限系统；
- 没有自动数据库降级迁移；
- Docker 执行器尚需在本机可访问的真实 Engine 上验收；
- 真实 GitHub 写操作仍未开放。

这些边界应在简历和面试中主动说明。Runbook 的价值是让部署和恢复过程可执行、可检查，而不是用“生产级”三个字代替证据。
