# CLAUDE.md

## 这个项目是什么

CoreCoder：用 ~1000 行纯 Python 复刻 Claude Code 核心骨架的极简 coding agent。

心脏是 `agent.py` 里的 while 循环：

```
用户消息 → LLM(带工具) → 有工具调用? → 执行 → 喂回 → 循环
                        ↘ 没有 → 返回文字给用户
```

子系统（每个都能一口气读完）：

- `agent.py` (150行) —— 主循环 + 多工具并行
- `tools/` —— 读/写/改/搜/跑命令/抓网页/看时间/开子 agent
- `llm.py` (336行) —— 接任意模型 + 重试 + 算钱
- `context.py` (210行) —— 三层上下文压缩
- `session.py` (100行) —— 会话存盘续聊
- `article/` —— 8 篇导读（00-07），是学习主线

## 我是谁 / 协作方式

- 我是 coding agent / LLM 应用领域的**初学者**，用这个项目找工作。
- 学习必须**由浅入深、循序渐进**，不要一上来就上并发/流式执行等高级主题。
- 讲概念先给直觉/类比再给代码，不要跳步、不要堆术语。
- 母语中文。

## 学习路线（由浅入深，按顺序走，别跳）

1. **读懂骨架** —— 能复述 `agent.py` 的循环（article 01-02）
2. **把"正确"做实** —— 补测试、啃边界：孤儿 tool 消息 / 参数绑定失败 / 中断回填
3. **单点能力** —— 一次加一个小工具，每个都能讲清"为什么这么做"
4. **可靠性/工程化** —— 可观测性、并发安全、成本控制（这里才叫"工业化"）
5. **面试深水区** —— 上下文为什么分层 / tool 安全为什么不是黑名单 / 子 agent 为什么不递归 / 流式 vs 批量（暂不碰）

## 重要提醒

- `问题点.txt` 里那 3 条并发疑问（边生成边执行 / 安全 vs 不安全并发 / 线程安全）
  属于第 4 层之后的进阶题，先读懂 article 05 再动手，不要提前硬啃。
- 判断我的学习进度以 git 状态 + article 阅读进度为准。

## 延伸方向（2026-08-16 确定）

在 5 层学习路线之外，agent 往后延伸有 5 个方向，按"现阶段该不该碰"排：

| 方向 | 是什么 | 难度 | 现阶段 |
|---|---|---|---|
| ① 工具生态 / MCP | 让 agent 动态接入外部工具（MCP 协议） | ★★☆ | ✅ 下一个做 |
| ② 评估 Evals | 量化"agent 干活质量好不好" | ★★☆ | ✅ 之后做 |
| ③ 上下文 + 成本 | prompt caching、精确 token、压缩质量 | ★★★ | 暂缓 |
| ④ 可靠性与可观测 | 日志/追踪/并发安全（`问题点.txt` 那三条） | ★★★★ | 暂缓 |
| ⑤ 多 agent 编排 | 子 agent 之上的任务分发 | ★★★★ | 暂缓 |

**结论：现在聚焦 MCP，接着是 Evals，③④⑤ 之后再说。**

MCP 第一小步：用 Python 官方 `mcp` SDK，让 CoreCoder 连上一个现成的 MCP server
（如官方 filesystem / GitHub server），让模型能调到它暴露的工具。
先理解「客户端/服务端、stdio 传输、JSON-RPC、动态工具发现」这一串概念，再动手。
